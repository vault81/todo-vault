[profile.dev.package."*"]
opt-level = 3
[profile.dev.package.app]
opt-level = 1
[profile.dev.package.server]
opt-level = 1
[profile.dev]
opt-level = 1
lto = "thin"

[profile.release]
codegen-units = 1
lto = "full"
opt-level = 3
strip = "debuginfo"

[workspace]
members = ["app", "entity", "migration", "server", "wasm-lib"]

[workspace.dependencies]
leptos = { version = "0.2.1", default_features = false, features = ["serde"] }
leptos_dom = { version = "0.2.1", default_features = false }
leptos_meta = { version = "0.2.1", default_features = false }
leptos_router = { version = "0.2.1", default_features = false }
leptos_axum = { version = "0.2.1" }

[[workspace.metadata.leptos]]
# The name used by wasm-bindgen/cargo-leptos for the JS/WASM bundle. Defaults to the crate name
name = "todo-vault"
output-name = "todo-vault"
site-root = "target/site"
assets-dir = "public"
site-pkg-dir = "pkg"
style-file = "style/main.sass"
site-addr = "127.0.0.1:3000"
bin-package = "server"
lib-package = "wasm-lib"
